#include <stdio.h>
int main()
{
    int x, y;
    printf("enter value of x   ");
    scanf("%d", &x);
    printf("enter value of y you want to swap with x   ");
    scanf("%d", &y);
    int X=y;
    int Y=x;
    printf("x=%d and y=%d", X,Y);
    return 0;
}

#include <stdio.h>
typedef float decimal;// it means that we have defined float as decimal and if i write decimal it will work as float 
int main()//basically typedef se predefined data types ka naam badalte h 
{
decimal a = 45.2;
float b = 78.9;
printf("%f,,%f", a,b);

// both data types will work
// refer to structure basics for more clearity
    return 0;
}